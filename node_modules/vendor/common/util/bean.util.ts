export class BeanUtil {
    static clone<T>(source: T): T {
        let originProto = Object.getPrototypeOf(source);
        console.log(originProto)
        let target=Object.assign(Object.create(originProto), source)
        console.log(target)
        return target;
    }
}